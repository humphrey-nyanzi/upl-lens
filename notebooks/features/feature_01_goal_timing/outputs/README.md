# Outputs

This folder is reserved for local notebook exports for Feature 1.

Committed reference images for the original pilot currently live in:

```text
outputs/features/feature_01_goal_timing/
```

Those images are evidence/reference artifacts. The production dashboard should
continue to read from FastAPI, not from exported notebook files.
